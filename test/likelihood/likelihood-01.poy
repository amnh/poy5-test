(* Script for mixing aligned and prealigned data; cost must be mpl *)
set (seed:1,timer:0)

read("FILENAME1")
read("FILENAME2")

transform(likelihood:(jc69,mpl,gap:(coupled)))
build(2,optimize:(model:never,branches:never))

report(all_roots)
report(lkmodel)
report(ia:all)
