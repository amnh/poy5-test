(* Static NCM Test; w/ 1 argument *)
set (seed:1, timer:0)
read(FILENAME1)
build(1,random)

transform(likelihood:(ncm,FILENAME2))

swap()

transform(parsimony)
transform(likelihood:(ncm, gap:character))
transform(parsimony)
transform(likelihood:(ncm))
